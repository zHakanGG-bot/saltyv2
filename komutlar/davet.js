










































































































































































































































































































































































































const Discord = require('discord.js');
const client = new Discord.Client();

exports.run = async (client, message) => {
  
  const db = require('quick.db');
  

  const davet = new Discord.RichEmbed()
.setColor("AEDD13")
  .setTimestamp()
.setAuthor(`${client.user.username}`, client.user.avatarURL)
.setDescription(`[Salty BOT](https://discordapp.com/api/oauth2/authorize?client_id=620986732464046080&permissions=8&scope=bot) \nMüzik komutları, moderasyon ve fazlası için davet et.`)
.addField("Davet Linki", "[BURAYA BAS : D](https://discordapp.com/api/oauth2/authorize?client_id=620986732464046080&permissions=8&scope=bot)")
  .setFooter(`© Tüm hakları saklıdır.`, client.user.avatarURL)
  message.channel.send(davet)
};

exports.conf = {
  enabled: true,
  guildOnly: false,
  aliases: ['linkler'],
  permLevel: 0,
    kategori: "bot",
  category: "bot"
};

exports.help = {
  name: 'davet',
  description: 'Botun davet linklerini gösterir.',
  usage: 'davet',
  enname: 'invite',
  endescription: 'Bot shows invitation links.',
  enusage: 'invite'
};